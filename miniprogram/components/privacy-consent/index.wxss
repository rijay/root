.privacy-overlay {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: flex;
  align-items: flex-end;
  background: rgba(8, 8, 6, 0.68);
}

.privacy-dialog {
  width: 100%;
  min-height: 458px;
  padding: 12px 20px calc(28px + env(safe-area-inset-bottom));
  box-sizing: border-box;
  color: var(--root-ink);
  background: var(--root-nav);
  border-radius: 20px 20px 0 0;
}

.privacy-handle {
  width: 52px;
  height: 4px;
  margin: 0 auto 28px;
  background: var(--root-line);
  border-radius: 2px;
}

.privacy-platform-tag {
  color: var(--root-moss);
  font-size: 12px;
  line-height: 18px;
  font-weight: 500;
}

.privacy-title {
  margin-top: 12px;
  font-size: 20px;
  line-height: 28px;
  font-weight: 600;
}

.privacy-copy {
  margin-top: 20px;
  color: var(--root-copy);
  font-size: 13px;
  line-height: 22px;
}

.privacy-link {
  margin-top: 12px;
  color: var(--root-moss);
  font-size: 13px;
  line-height: 22px;
}

.privacy-actions {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-top: 64px;
}

.privacy-button {
  width: 100%;
  height: 54px;
  min-height: 54px;
  margin: 0;
  padding: 0;
  border: 0;
  border-radius: 12px;
  font-size: 16px;
  line-height: 54px;
}

.privacy-button::after {
  border: 0;
}

.privacy-button--primary {
  color: #fff;
  background: var(--root-moss);
  font-weight: 600;
}

.privacy-button--secondary {
  color: var(--root-muted);
  background: transparent;
  font-size: 14px;
}
