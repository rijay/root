<view class="page">
  <view class="content-card">
    <view class="eyebrow">HEALTH PROFILE</view>
    <view class="h1">我的标签 / 健康画像</view>
    <view class="body">这些信息用于个性化欢迎与后续顾问沟通。</view>
  </view>
  <view class="section content-card">
    <view wx:for="{{rows}}" wx:key="label" class="list-item">
      <view class="section-title">{{item.label}}</view>
      <view class="body">{{item.value}}</view>
    </view>
    <view wx:if="{{!rows.length}}" class="body">暂未完成画像问卷。</view>
  </view>
</view>
