# myRoot 正式提审检查点（2026-08-04）

## 1. 管理结论

当前代码已经达到“可准备新的候选环境和真机验收”的状态，但还不能等同于“可以直接提交微信审核”。

- `main` 已合并到 `f8e12966df16bc339b558bf8ce47cea5a246c23a`；
- 本地正式上线门禁为 `6/6 PASS`；
- 小程序代码版本为 `0.5.17`，发布源码清单已生成并通过静态预算；
- 生产 CloudBase 环境和稳定后端当前可用，MySQL 已到迁移 `068`；
- 当前 `main@f8e12966` 后端工件已绑定到经过回读的 `myroot-api-045 / URL_PARAMS / 0%` 候选；
- 小程序真机性能、Admin 候选查询与浏览器性能、微信平台开发版/审核/发布状态仍需独立取证。

因此，下一阶段只处理当前首发链路，不重新解耦或维护已经退役的旧功能。

## 2. 本轮已确认事实

| 项目 | 当前证据 | 结论 |
| --- | --- | --- |
| Git 基线 | `origin/main@f8e12966`；提审准备分支只增加检查点材料与本地门禁修正 | 通过 |
| 仓库验证 | 后端、Admin、小程序范围与性能、本地正式证据共 `6/6 PASS` | 通过 |
| 小程序回归 | 完整 `npm run check --prefix miniprogram` 通过；登录、我的、Root4U、活动高保真契约分别为 `28/28`、`39/39`、`33/33`、`36/36 PASS` | 通过 |
| Admin 构建 | 生产构建成功；初始、首屏、总压缩资源均低于目标预算 | 本地通过 |
| 小程序源码 | `0.5.17`；132 个发布文件；329,719 bytes | 本地通过 |
| 小程序源码清单 | SHA-256 `caed4cd79b4ebf719b23f0df08957df568af75afbd3d928764818d4665ffb67e` | 已固定 |
| CloudBase 环境 | `myroot-prod-d5gl3gzg7115f149a` 为 `NORMAL`；`myroot-api` 服务为 `normal` | 运行可用 |
| 稳定后端 | `/health`、`/ready` 返回成功；稳定响应版本为 `0.5.13`，releaseId 已配置 | 通过只读探针 |
| MySQL | 连接正常，迁移为 `068_formal_launch_confirmed_prelaunch_cleanup.sql`，最小权限就绪 | 通过只读探针 |
| 当前后端候选 | `myroot-api-045 / URL_PARAMS / 0%`；releaseId 为 `v1.0.0+f8e12966-formal-health-write-candidate-20260805` | 通过候选创建、流量保护与 Root4U 写链路回验 |
| 微信开发工具 | 登录有效；本次 `main` worktree 已导入 | 工具可用 |
| 微信局部编译 | 单文件编译调用未在合理时间内返回，已主动终止 | 不计为通过 |

## 3. 候选材料与部署结果

候选材料已用于创建 0% 条件候选；本机仍保留以下可复核材料：

| 材料 | 结果 |
| --- | --- |
| CloudRun ZIP | `/tmp/myroot-api-candidate-f8e12966.zip` |
| ZIP SHA-256 | `aaed975da6daa0c688c5f5e3b7e331b8112a51ed5f6c20ba4c8d3b987dc0b43c` |
| ZIP 条目 | 292 个条目；280 个文件 |
| 解包源码清单 | SHA-256 `34a6347bd0a600a4e51c0829adec04c1a98e32c85a0b062d2add6f081426cada` |
| 来源提交 | `f8e12966df16bc339b558bf8ce47cea5a246c23a` |
| 来源证明 | `VERIFIED_LOCAL_PROVENANCE_FOUNDATION` |
| 来源归档 SHA-256 | `62de74fec3b0b3cac57a8968aa6fe6e99d5483754ed2661324468d6c29a75174` |

部署与回验结果：

| 项目 | 结果 |
| --- | --- |
| 执行时间 | `2026-08-04 23:26-23:38 +08:00` |
| 已结束旧任务 | 仅取消仍活动的 `myroot-api-043 / URL_PARAMS / 0%` 灰度任务；未删除历史版本 |
| 新候选 | `myroot-api-044 / normal` |
| 发布单 | `041 -> 044 / URL_PARAMS / flowRatio=0 / grayStatus=success` |
| 稳定默认版本 | `myroot-api-041`，控制台保持 `100%` |
| 候选来源 | `main@f8e12966df16bc339b558bf8ce47cea5a246c23a` |
| 候选 releaseId | `v1.0.0+f8e12966-formal-review-candidate-20260804` |
| 候选配置 | 81 个环境变量键保持一致，仅更新 releaseId 与工件摘要；VPC、端口、CPU、内存和实例范围回读无漂移 |
| 定向 `/health` | HTTP 200、业务码 0、`version=0.5.13`、releaseId 匹配 044 |
| 定向 `/ready` | HTTP 200、MySQL connected、迁移 068、schema 级最小权限就绪 |
| 默认流量保护 | 15/15 无候选路由请求返回稳定 releaseId；044 命中 0 次 |
| 045 执行时间 | `2026-08-05 00:17-00:25 +08:00` |
| 045 变更范围 | 复用同一 ZIP；在 044 配置上只增加 `ROOT_FORMAL_HEALTH_WRITES_ENABLED=true`，并更新候选追踪 releaseId |
| 045 发布单 | `041 -> 045 / URL_PARAMS / flowRatio=0 / grayStatus=success`；仅结束 044 的活动 0% 灰度任务，未删除历史版本 |
| 045 配置 | 82 个环境变量；VPC、端口、CPU、内存和实例范围与 044 一致 |
| 045 运行回验 | 定向 `/health`、`/ready` 均返回 045 releaseId；15 次默认请求中 045 命中 0 次 |

条件路由值仅保存在本次受控会话内，未写入仓库或本文件。上述 0% 表示候选只接受命中 URL 参数规则的定向请求，不获得随机百分比流量。

## 4. 044 候选只读链路回验

执行时间：`2026-08-04 23:40-23:46 +08:00`。本轮共执行公开读取、未登录保护检查、Admin 读取和只读延迟诊断；没有创建会话、用户、授权、问卷结果或后台草稿。

| 范围 | 实际结果 | 结论 |
| --- | --- | --- |
| 候选绑定 | 发布顺序仍为 `041 -> 044`；044 为 `URL_PARAMS / 0%`；releaseId 与候选一致 | 通过 |
| 公开接口 | `/health`、`/ready`、隐私说明、欢迎页、首页、活动均为 HTTP 200、业务码 0 | 通过 |
| 未登录保护 | 用户状态、Root4U、初始化建档结果、活动报名状态均为 HTTP 401、业务码 1003，且没有 token | 通过 |
| 无身份登录 | HTTP 200、业务码 1007、没有 token；属于当前业务错误返回约定，仍为 fail-close | 通过 |
| Admin 读取 | 14 个代表接口全部为 HTTP 200、业务码 0 | 通过 |
| Root4U 初始化建档 | 12 题，状态 `PUBLISHED`，引导选项 1 组 | 通过只读回验 |
| 固定建议兜底 | 当前代码仍由固定内容 Adapter 生成评分后的生活方式建议；推荐规则和模型策略不是最小闭环的强依赖 | 代码确认；真实用户写链路待验 |
| 欢迎页 | Admin 有 2 个屏幕但状态均为 `EMPTY`；公开接口为 0 屏 | 未发布，符合本轮范围 |
| 首页与活动 | 公开首页轮播 0 项；公开活动 0 项 | 未发布，符合本轮范围 |

Admin 只读诊断各采集 20 次，结果如下。它用于提前发现明显回归，不替代要求固定数据集、详情、写入和浏览器证据的正式 Admin 性能 Gate。

| 场景 | 失败数 | P75 | P95 | 最大响应体 |
| --- | ---: | ---: | ---: | ---: |
| 后端健康检查 | 0 | 29 ms | 30 ms | 265 bytes |
| 首页轮播列表 | 0 | 104 ms | 108 ms | 95 bytes |
| 健康量表列表 | 0 | 103 ms | 107 ms | 273 bytes |
| 活动列表 | 0 | 102 ms | 117 ms | 133 bytes |
| 审计日志 | 0 | 99 ms | 104 ms | 7,246 bytes |

本地性能复核：

- 小程序主包 `225,704 bytes`、总包 `329,719 bytes`，静态包体预算通过；尚无真机旅程事件；
- 已修正候选性能报告的空样本误判：没有事件时现在返回 `BLOCKED_MISSING_SAMPLES`、`releaseGateEligible=false` 并以非零状态退出；
- Admin 初始资源 `227,292 bytes`、首屏 `227,621 bytes`、总压缩资源 `282,660 bytes`，构建 Gate 通过；候选查询和浏览器 Gate 仍明确为 `BLOCK`。

## 5. 044 阻断与 045 Root4U 真实闭环

执行时间：`2026-08-04 23:48` 至 `2026-08-05 00:22 +08:00`。本次按授权使用已确认的专用测试号码继续闭环；身份、资料与单独同意已完成，12 问写入被 044 的健康写入 Gate 明确拒绝，没有形成部分评测结果。

| 步骤 | 实际结果 | 状态 |
| --- | --- | --- |
| 候选保护 | 写入前再次确认 `041 -> 044 / URL_PARAMS / 0%`，定向 `/health` releaseId 匹配 044 | 通过 |
| 微信身份登录 | 使用开发者工具真实 `wx.login` 在 044 成功签发 1 个会话；账号为既有账号，返回 `PROFILE_REQUIRED` | 已写入会话 |
| 暴露会话撤销 | 通过无触发器、目标固定的一次性 VPC 函数，对唯一会话摘要先预检再 CAS 撤销；旧令牌调用 044 返回 HTTP 401、业务码 1003 | 通过 |
| 一次性通道清理 | 临时函数删除后回读不存在；临时数据库账号回读数量为 0 | 通过 |
| 专用账号条件 | 平台号码 `139****8817` 经确认后重新发起标准手机号授权；新会话进入 `PROFILE_REQUIRED` | 通过 |
| 登录失败恢复 | 真机预演发现失败弹窗会复用单次手机号授权码；已改为关闭提示后重新点击登录按钮，确保平台签发新码 | 本地修复，待下一候选验证 |
| 最小资料 | 使用默认昵称和头像策略，写入成年测试生日与测试性别；回到首页 | 通过 |
| 健康同意 | 当前版本身体反馈与健康记录单独同意已写入并回读为生效 | 通过 |
| 初始化建档 | 12 个合成测试答案已通过小程序提交方法发送到 044；传输 HTTP 200，但业务层返回 `FORMAL_HEALTH_WRITES_DISABLED` | 阻断；评测仍为 `NOT_STARTED`，没有答案结果落库 |
| 044 配置事实 | 044 的 81 个候选环境变量中没有 `ROOT_FORMAL_HEALTH_WRITES_ENABLED=true` | 已确认 |
| 临时材料 | 候选条件路由值未写入仓库；一次性密钥配置已删除 | 已清理 |
| 流量与内容 | 041/044 发布单保持不变；未发布欢迎页、首页或活动 | 未改变 |

045 经明确授权后复用同一后端 ZIP 建立，仅增加健康写入 Gate 并保持 0% 条件路由。专用账号闭环结果：

| 步骤 | 045 实际结果 | 状态 |
| --- | --- | --- |
| 写入前保护 | 账号为 `ELIGIBLE`、健康同意有效、评测状态 `NOT_STARTED`；定义为 12 题、已有答案 0 | 通过 |
| 单次提交 | 仅提交 1 次、共 12 个合成测试答案；安全题为无风险信号 | 通过 |
| 建档结果 | 回读 `assessmentState=COMPLETED`、问卷版本 2、完成时间存在 | 通过 |
| 评分分类 | `肠道规律关注型`，标签 1 个 | 通过 |
| 安全分流 | `STANDARD_GUIDANCE` | 通过 |
| 固定建议 | `adviceSource=FIXED_CONTENT`，返回 3 条 tips | 通过 |
| 后续推荐 | 返回 2 个评测推荐；没有依赖模型 API 才完成最小闭环 | 通过 |
| 内容保护 | 欢迎页 0 屏、首页轮播 0 项、公开活动 0 项 | 未发布 |
| 流量保护 | `041 -> 045 / URL_PARAMS / 0%`；15 次默认请求中 045 命中 0 次 | 通过 |

由此，“建档—评分—安全分流—固定建议”最小后端闭环已在隔离的生产候选上完成；这不等同于真机性能、微信开发版或审核 Gate 已通过。

## 6. 当前提审 Gate

| Gate | 状态 | 还需要什么 |
| --- | --- | --- |
| 代码与 CI | 通过 | 保持 `main` 与候选来源一致 |
| 小程序静态包体 | 本地通过 | 与微信开发工具实际构建产物交叉验证 |
| 小程序真机性能 | 阻断 | 当前为 `BLOCKED_MISSING_SAMPLES`；补齐冷/热启动、关键旅程、弱网和宽屏真机证据 |
| Admin 构建性能 | 本地通过 | 不能替代候选查询和浏览器证据 |
| Admin 候选查询性能 | 阻断 | 在新候选上采集；涉及专用草稿写入时另行授权 |
| Admin 候选浏览器性能 | 阻断 | 在同一候选、同一提交和同一 releaseId 上采集 |
| 当前 `main` 后端候选 | 通过 | `045 / URL_PARAMS / 0%` 已创建；同一工件、releaseId、路由、数据库、健康写入 Gate 和默认流量保护均已回读 |
| Root4U 真实用户闭环 | 通过 | 专用账号已完成手机号、资料、单独同意、12 问、评分、标准安全分流和三条固定建议；原暴露会话已撤销 |
| 微信开发版 | 待平台回读 | 当前 `main` 与既有开发包并非同一份可证明源码，需决定下一上传版本 |
| 微信审核与发布 | 待平台回读 | 上传、提交审核、审核通过、发布分别留证 |

## 7. 推荐执行顺序

1. 使用专用 Admin 草稿完成候选查询写入样本和 Chrome 浏览器性能采集；Edge 不在首发范围。
2. 用 iOS 与 Android 真机补齐小程序冷/热启动、关键旅程、弱网、帧率和内存证据，并交叉验证微信构建包体。
3. 从最终提交重新上传微信开发版。建议使用新的明确版本号，避免把此前上传记录与 `main@f8e12966` 混为同一包。
4. 回读公众平台开发版、体验版和审核准备项后，再决定是否提交审核。

## 8. 本轮未执行

- 未改变稳定版本、默认流量或灰度百分比；
- 已按授权精确撤销暴露会话，并为专用测试账号完成手机号登录、最小资料与当前版本健康单独同意；
- 045 已按授权写入 1 份专用账号的 12 问建档结果；没有重复提交、没有写入其他用户健康数据；
- 未写入 Admin 性能测试草稿；
- 未上传小程序、未提交审核、未发布；
- 未发布欢迎页、首页或活动内容。
