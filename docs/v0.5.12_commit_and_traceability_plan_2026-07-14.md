# v0.5.12 提交与远端追溯计划

日期：2026-07-14

状态：`SPLIT_READY / NO_NEW_COMMIT / NO_TAG / NO_PUSH`

## 1. 实际读取来源

1. 当前工作树的已修改与未跟踪文件。
2. `main`、`origin/main`、现有远端和 tag 列表。
3. 027 的部署来源提交、`backend` 树、原始 ZIP 与本地持久化清单。
4. 小程序 Route Module、上传保护和对应测试差异。

当前 `main` 位于 `ef9fab9`，比 `origin/main` 领先 10 个提交；`ef9fab9` 没有被任何远端分支或 tag 包含。本计划不执行提交、tag 或 push。

## 2. 提交拆分

### Commit A：小程序候选路由生命周期修复

建议消息：`fix: refresh mini program canary route on resume`

文件：

- `miniprogram/app.js`
- `miniprogram/project.config.json`
- `miniprogram/scripts/cloud-route.test.js`
- `miniprogram/scripts/validate.js`
- `miniprogram/utils/cloud-route.js`
- `package.json`
- `scripts/generate-wechat-trial-qrcode.js`
- `scripts/generate-wechat-trial-qrcode.test.js`
- `scripts/final-verification.js`

职责：

1. 小程序从后台被普通小程序 `getQRCode(env_version=trial)` 生成的带参小程序码唤起时，通过 `App.onShow` 刷新当前进程的候选路由。
2. 普通回前台保留当前会话路由，非法参数清空，正式版始终拒绝候选路由。
3. 微信上传明确忽略嵌套 `.git`，最终验收同步提高忽略规则计数。
4. trial 小程序码 CLI Adapter 默认 dry-run，使用普通小程序 `getQRCode(env_version=trial)` 与隔离的 `stable_token(force_refresh=false)`；真实凭据、token 和完整候选值均不进入参数或输出。

### Commit B：v0.5.12 上线证据与外部接入门禁

建议消息：`docs: record v0.5.12 launch evidence and gates`

文件：

- `.gitignore`
- `DEPLOY.md`
- `docs/adapter_calibration_playbook.md`
- `docs/checkin_reminder_subscription_design.md`
- `docs/checkin_reminder_027_retest_readiness_2026-07-14.md`
- `docs/external_adapter_activation_v0.5.12_2026-07-13.md`
- `docs/formal_launch_checkpoint_2026-07-12.md`
- `docs/formal_launch_gate_v0.5.12_2026-07-13.md`
- `docs/release_notes_v0.5.12.md`
- `docs/release_readiness.md`
- `docs/cloud_functions_v0.5.12_preflight_2026-07-14.md`
- `docs/miniprogram_v0.5.12_trial_preflight_2026-07-14.md`
- `docs/production_cutover_proof_preflight_v0.5.12_2026-07-14.md`
- `docs/production_gray_release_027_2026-07-13.md`
- `docs/release_artifact_manifest_v0.5.12_2026-07-14.md`
- `docs/youzan_application_approval_v0.5.12_2026-07-14.md`
- 本计划

职责：

1. 记录 027 的 0% 部署、运行 Gate、Function/体验版预检与工件链路。
2. 修正有赞回调假前置，增加消费者隐私数据加密/解密门禁。
3. 记录 CLI 登录缺口、本地工件保全与仍未关闭的正式上线事项。

## 3. 验证顺序

1. Commit A 前运行小程序专向检查与 Route Module 场景测试。
2. Commit A 后运行完整 `npm run verify`，要求 `16/16 PASS`，并确认 trial 小程序码无网络契约测试包含在结果内。
3. Commit B 前核对所有证据引用存在，运行 `git diff --check`。
4. Commit B 后确认工作树干净，并再次核对部署来源提交 `ef9fab9` 的 `backend` 树仍为 `9e1525824c58f0807b3079239381ab6e9b45b07b`。
5. 核对最终提交的 `backend` 子树与部署来源一致；若不一致，停止远端追溯流程并解释差异。

## 4. 远端发布顺序

远端写入必须另行确认，建议分成三个动作：

1. 推送一个专用候选分支 `release/v0.5.12-027`，不直接移动 `origin/main`。
2. 用远端提交永久链接关闭 T-012 的证据耐久性缺口。
3. 同版本真机、提醒和其余正式 Gate 完成后，按仓库既有轻量 tag 习惯创建 `v0.5.12`，再单独确认推送 tag 和合并主分支。

027 的真实部署来源继续记录为 `ef9fab932a08cb2f48f63b04605e6ac9c94c8c19`；后续证据提交不能伪装成原始部署提交。只要该提交成为远端候选分支的祖先，且最终提交的 `backend` 子树保持一致，就能同时保留部署来源与证据演进记录。

## 5. 工件远端化

1. 本地保全 ZIP：`release-artifacts/v0.5.12/myroot-api-v0.5.12-ef9fab932a08.zip`。
2. 原始 SHA-256：`ff4491fafa36f8dc68b12593c46ac258397c24bce89c780228d6aa1242b586cc`。
3. 二进制已被 `.gitignore` 排除，不随候选分支进入普通 Git 历史。
4. 推送候选分支后，再单独决定 GitHub Release 或受控对象存储作为团队工件位置。
5. 上传后必须重新下载并核对哈希，才能形成 T-015 的永久 `evidenceRef`。

## 6. 回退

1. 本地提交若内容有误，不使用破坏性重置；在尚未 push 时追加修正提交或按用户明确指令处理。
2. 候选分支 push 后不强推；错误内容通过新提交修正。
3. tag 只在最终版本内容固定后创建；若 tag 尚未 push，可在明确确认后调整，已 push 则不静默重写。
4. 工件上传失败不改变 027 流量；保留本地原件并停止 T-015 写入。

## 7. 对抗式审查

1. **把证据提交说成部署提交**：禁止。027 的部署来源固定为 `ef9fab9`。
2. **直接推 main 扩大影响**：先推专用候选分支，主分支合并单独决定。
3. **过早创建正式 tag**：T-013、T-011 等仍未完成前不创建 `v0.5.12`。
4. **把 ZIP 放进 Git 历史**：二进制保持忽略，使用专用工件位置。
5. **测试全绿掩盖外部门禁**：`16/16 PASS` 只证明本地 Interface，没有关闭有赞、企微、物流、真实提醒、真机或灰度 Gate。

## 8. 结论

当前差异可以按“运行代码与发布工具修复”和“上线证据/门禁”拆成两个可审查提交。下一步只有在取得明确确认后才创建这两个提交；远端候选分支、T-012 写入、工件上传、正式 tag 和主分支合并继续分别授权。
