# myRoot v0.6.0 内部发布门禁

> 日期：2026-08-25
> 适用对象：当前本地候选代码
> 状态：`REVIEW_SUBMITTED_OWNER_CONFIRMED / REVIEW_PENDING`；当前候选已于 12:05 上传成功并完成提审前人工门禁
> 边界：提交结果来自项目负责人确认，本记录尚未独立回读平台审核状态或审核单号，不代表已审核通过或已正式发布。

## 1. 本轮正式口径

- 活动页不得内置、注入或在接口异常时回退到本机模拟活动；
- 活动列表、详情、报名和取消只使用服务端权威接口；
- 服务端没有已发布活动时展示合法空状态，接口异常时展示错误和重试入口；
- 完成肠道 5 道题自测的用户可前往 ROOT 会员商城领取 5 支益生元体验装；
- 体验装已确认上架且可领取，库存有限、领完即止，最终领取结果以商城页面为准；
- 会员商城固定短链保持为 `#小程序://ROOT会员商城/n3slzlsfIydORAd`。

## 2. 自动门禁

| Gate | 准入条件 | 当前结果 |
|---|---|---|
| 活动模拟数据 | 发布代码不存在本机模拟活动文件、标识或本地报名分支 | `PASS` |
| 活动权威数据 | 列表和详情读取服务端；报名、取消通过服务端写接口执行 | `PASS` |
| 活动失败状态 | 接口异常不生成假活动，保留错误状态和重试 | `PASS` |
| 体验装正式文案 | 结果页和陪伴计划页统一为已上架、库存有限口径 | `PASS` |
| 体验装跳转 | 固定 ROOT 会员商城短链仍被配置和测试锁定 | `PASS` |
| 体验装按钮布局 | 领取按钮占满卡片可用内宽、固定高度并保持单行 | `PASS` |
| 体验版/正式版运行配置 | 两者使用相同生产接口、关闭本地兼容数据，健康记录仅保存在本机 | `PASS` |
| 本机健康数据与隐私 | 健康记录本机保存、最长 180 天、不上传 myRoot 服务端的实现和文案一致 | `PASS` |

自动门禁由 `miniprogram/scripts/internal-release-gate.test.js` 及现有完整 `npm run check` 共同执行。任一项失败时不得上传新的提审候选。

## 3. 人工门禁

| Gate | 当前结果 | 证据边界 |
|---|---|---|
| 体验版真机验收基线 | `PASS_OWNER_CONFIRMED` | 项目负责人于本轮明确确认已执行完毕；本记录未重复读取设备、录屏和明细 |
| 当前候选受影响范围冒烟 | `PASS_OWNER_CONFIRMED` | 项目负责人确认 12:05 当前体验版已完成真机冒烟；本记录未重复读取设备、录屏和明细 |
| 微信公众平台人工回读 | `PASS_OWNER_CONFIRMED` | 项目负责人于本轮明确确认已执行完毕；本记录未重新登录公众平台复核 |
| 体验装上架、可领取与库存 | `PASS_OWNER_CONFIRMED` | 项目负责人于本轮明确确认；库存为动态外部状态，页面仍以商城最终结果为准 |

本轮未改变平台隐私声明、权限或类目，因此公众平台人工回读不因本次代码改动自动失效。当前候选的受影响范围冒烟已由项目负责人确认完成。

## 4. 上传/提审前最后检查

1. 运行 `cd miniprogram && npm run check`，必须全部通过；
2. 确认待上传目录为当前候选目录，不包含本机模拟活动文件；
3. 在微信开发者工具中编译一次，检查活动空态/错误态和体验装领取跳转；
4. 上传后记录版本号、上传时间、描述和代码包信息；
5. 在公众平台重新确认本次新上传版本被正确设置为体验版；
6. 提交审核和正式发布仍需单独执行，不由本门禁记录自动授权。

## 5. 2026-08-25 执行记录

截至 2026-08-25 10:51 CST：

- 微信开发者工具登录账号可管理 AppID `wx7727a02565aed1c2`，对应“myRoot会员体验中心”；
- 生产活动接口 `/api/v1/activities?pageSize=20` 返回 HTTP 200；
- 当前公开活动为 `ROOT X Weekly Sports · City Walk 你的每一步，都算数`，状态为 `PUBLISHED / PUBLIC / COMING_SOON`，页面文案未出现“模拟、内部测试、预览”等词；
- 活动详情已只读回读，活动目标、人群、流程、主办方、费用、携带物品、取消说明、隐私和拍摄说明均有正式内容；
- 因该场次尚未开放报名，本轮未修改活动状态，也未发起报名或取消写入；既有报名真机验收基线继续保留；
- `npm run check` 全量通过；活动列表、活动详情、评测结果和陪伴计划页的 WXML/WXSS 局部编译全部通过；
- 已发起一次 `0.6.0` 体验版上传确认任务 `confirmation_upload_dfb86f86-5a8e-4185-b826-894e195aba90`，但确认任务在回读时返回 `Task not found`；本次不认定为上传成功，且未自动重试；
- 项目负责人随后明确批准仅重试一次上传；2026-08-25 10:52 CST 通过微信开发者工具 CLI 将 `0.6.0` 上传成功，目标 AppID 为 `wx7727a02565aed1c2`，描述为“提审候选：正式活动、肠道自测与体验装领取”；
- 上传代码包总大小 `2,109,736` bytes，其中主包 `858,592` bytes；上传信息保存在本机临时证据 `/private/tmp/myroot-v060-upload-retry-20260825.json`；
- 活动页手机预览已成功推送至当前开发者微信，用于受影响范围冒烟；
- 该候选后来已被 12:05 上传的健康导航修正版取代；当前硬门禁以 5.6 和第 6 节为准。

### 5.1 体验装领取按钮缺陷修复

截至 2026-08-25 11:01 CST：

- 10:52 已上传候选在真机结果页出现领取按钮过窄、过高和文字换行，实测等效布局约为 `184 × 114px`；
- 根因是微信原生 `button` 样式使既有 `width: 100%` 未按卡片可用内宽生效；
- 本地候选已使用卡片限定选择器、`width: 100% !important`、固定 `96rpx` 高度、Flex 居中和 `white-space: nowrap` 修复；
- 微信开发者工具模拟器实测卡片为 `366 × 246px`，按钮为 `332 × 49px`，计算宽高分别为 `332px / 49px`，文字保持单行；
- 结果页 WXSS 编译通过，`npm run check` 全量通过；截图证据保存在 `/private/tmp/myroot-v060-trial-button-fixed.png`；
- 10:52 已上传候选不含本修复，已被本地候选取代，不得直接提交审核；必须重新上传后做一次真机按钮和商城跳转冒烟。

### 5.2 产品 tab 顶部导航一致性修复

截至 2026-08-25 11:09 CST：

- 产品 tab 原先仍使用微信原生导航栏，形成独立白色标题条，与首页、健康、活动、我的采用的自定义沉浸式顶部不一致；
- 本地候选已将产品页切换为 `navigationStyle: custom`，移除原生标题条，并将页面和顶部区域统一为 ROOT 导航底色 `#FDFBF6`；
- 页面内“产品”标题与活动/我的 tab 对齐为顶部 `78px`、左侧 `20px`、字号 `30px`、行高 `38px`；原产品卡片、购买、详情、分享和位置恢复逻辑未改；
- 产品页 WXML/WXSS 局部编译通过，模拟器样式实测通过，`npm run check` 全量通过；截图证据保存在 `/private/tmp/myroot-products-header-fixed.png`；
- 10:52 已上传候选同样不含本修复，后续须与体验装按钮修复一起重新上传并做受影响范围真机冒烟。

### 5.3 产品 tab 信息精简

截至 2026-08-25 11:13 CST：

- 本地候选已移除产品 tab 顶部“全部产品，按你的节奏来选”；
- 商品卡片已移除“进一步了解”入口和“会员中心实时价格”展示，购买入口保留；
- 产品页底部已移除“本地跳转配置更新于 2026-08-25”展示；商品跳转配置及有效性校验保留；
- 删除次级入口后，购买按钮调整为卡片内容区全宽，模拟器实测为 `286 × 45px`；
- 模拟器查询确认 `.products-title`、`.product-learn`、`.product-price` 均无实例；产品页 WXML/WXSS 编译及 `npm run check` 全量通过；
- 截图证据保存在 `/private/tmp/myroot-products-simplified.png`；该改动仍未上传，须与前两项本地修复合并为新的体验版候选。

### 5.4 活动详情底栏状态去重

截至 2026-08-25 11:24 CST：

- `COMING_SOON` 原先同时映射为左侧活动状态和右侧禁用按钮文案，导致底栏重复显示两个“即将开放”；
- 本地候选已保留左侧“即将开放”，并显示弱提示“报名尚未开放，请稍后再来查看。”；`action.kind === NONE` 时不再渲染右侧禁用按钮；
- 可报名、登录、取消报名等可操作状态仍渲染原操作按钮，活动状态与服务端数据结构未改；
- 模拟器确认 `.detail-action-button` 数量为 `0`、状态和弱提示分别正确；WXML/WXSS 编译、活动专项测试及 `npm run check` 全量通过；
- 截图证据保存在 `/private/tmp/myroot-activity-detail-coming-soon-fixed.png`；该改动尚未上传体验版。

### 5.5 新体验版上传结果

截至 2026-08-25：

- 上传前 `npm run check` 全量通过，微信开发者工具登录有效，项目 AppID `wx7727a02565aed1c2` 与“myRoot会员体验中心”一致；
- 已发起 `0.6.0` 上传确认任务 `confirmation_upload_0fb141f1-1636-4f95-802f-38b2f16b3c45`，描述为“提审候选：体验装按钮、产品页与活动底栏修复”；
- 回读任务时返回 `Task not found`，本次不认定为上传成功，也未自动重试；
- 项目负责人随后明确批准重新上传一次；2026-08-25 11:36 CST 发起确认任务 `confirmation_upload_7a4c65b6-48d0-4b97-b45a-bad6ccfb1b33`，任务最终状态为 `success`、结果为 `execution_success`；
- 本次代码包总大小 `2,109,339` bytes，其中主包 `857,572` bytes，已包含 5.1–5.4 的体验装按钮、产品页和活动底栏修复；
- 11:36 上传版本当时为最新已确认候选，后续已被 5.6 记录的健康导航修正版上传取代。

### 5.6 健康 tab 顶部导航基线修复

截至 2026-08-25：

- 健康页原 ROOT 字标直接使用 `top: 74px / height: 22px`，视觉中心比产品、活动、我的统一的 `top: 78px / height: 38px` 标题行更高；
- 本地候选已增加统一标题容器，使用 `left: 20px / top: 78px / height: 38px`，并将 ROOT 字标在容器内垂直居中；
- 微信开发者工具实测标题容器为 `350 × 38px`、坐标为 `left: 20 / top: 78`，ROOT 字标最终坐标为 `top: 86`；健康页 WXML/WXSS 编译与 `npm run check` 全量通过；
- 模拟器截图证据为 `/private/tmp/myroot-health-header-aligned.png`；本修正发生在 11:36 体验候选上传之后，尚未重新上传。
- 随后发起 `0.6.0` 上传确认任务 `confirmation_upload_2e19076b-f298-4e76-a4d4-d162c15bc38b`，描述为“提审候选：健康Tab顶部导航对齐”；任务回读返回 `Task not found`，本次不认定为上传成功，且未自动重试。
- 项目负责人明确批准仅重试一次；2026-08-25 12:05 CST 改用微信开发者工具官方 CLI 上传同一候选，官方返回 `Using AppID: wx7727a02565aed1c2` 与 `✔ upload`，本次认定上传成功；
- 本次代码包总大小 `2,109,527` bytes，其中主包 `857,760` bytes；官方上传信息保存在 `/private/tmp/myroot-v060-health-header-upload-20260825.json`；
- 当前最新已确认上传成功的候选为本次健康导航修正版。

## 6. 当前提审结论

截至 2026-08-25 12:05 CST，当前候选身份如下：

- 版本：`0.6.0`；
- AppID：`wx7727a02565aed1c2`；
- 上传描述：`提审候选：健康Tab顶部导航对齐`；
- 代码包总大小：`2,109,527` bytes，其中主包 `857,760` bytes；
- 本地源目录：`/Users/rijay/Documents/Root/myroot-v060-slim-20260820/miniprogram`；
- 官方 CLI 证据：`/private/tmp/myroot-v060-health-header-upload-20260825.json`。

以下两项提审硬门禁已于 2026-08-25 由项目负责人确认完成：

1. 在微信公众平台“版本管理”中确认 12:05 上传版本已被选为当前体验版；
2. 使用当前体验版完成一次受影响范围真机冒烟：
   - 健康 tab 顶部 ROOT 字标与其他 tab 标题基线一致；
   - 评测结果页“前往领取 5 支体验装”按钮保持单行、全宽且能打开 ROOT 会员商城；
   - 产品 tab 顶部风格和精简文案正确，购买跳转可用；
   - 活动详情底部仅显示一个“即将开放”。

当前内部门禁状态为 `REVIEW_SUBMITTED_OWNER_CONFIRMED / REVIEW_PENDING`。项目负责人已确认手动提交微信审核；审核单号和平台状态尚未由本记录独立回读。

### 6.1 提审执行状态

- 项目负责人已确认当前体验版选择正确并完成真机冒烟；
- 项目负责人已明确授权提交微信审核；
- 自动控制微信公众平台管理页面被浏览器安全策略阻止，未通过自动控制改变平台状态；
- 项目负责人随后确认已在公众平台手动提交审核；
- 当前等待微信审核结果；审核单号、平台显示状态和提交时间仍待平台截图或状态页回读补充；
- 审核通过不等于正式发布，正式发布仍需单独批准。
