# myRoot v0.6.0 → 下一版本交接说明

## 1. 开发起点

下一版本必须从 Git 归档基线开始，不得继续以 `/Users/rijay/Documents/Root/myroot-v060-slim-20260820` 作为开发目录。该目录是提审源快照，没有 Git 历史，只保留作证据和差异核对。

当前推荐起点：

```bash
cd /Users/rijay/Documents/Root/root_seven_day_checkin
git fetch --all --prune
git switch --track origin/codex/v0.6.0-review-archive-20260825
```

在开始新需求前，先处理第 2 节的版本状态选择。

注意：标签 `archive/v0.6.0-review-submitted-20260825` 是已经提交微信审核的原候选；当前归档分支头还包含提审后本地调整，尚未上传。审核问题排查应优先对照该标签，后续新候选开发才使用分支头。

## 2. 先确认 v0.6.0 最终状态

### 审核未结束

- 仅在归档分支修复微信审核明确反馈的问题；
- 修复提交需单独记录原因、影响页面、新上传时间和审核状态；
- 不混入下一版本需求。

### 审核通过但未正式发布

- 先决定是否正式发布；
- 正式发布需要单独批准；
- 发布成功后补充平台版本、发布时间和状态证据，再创建正式版本标签。

### 已正式发布

- 将确认发布的归档提交合入主干；
- 创建正式标签 `v0.6.0`；
- 从主干创建下一版本分支，例如 `codex/v0.6.1-<scope>`。

### 审核被拒或决定放弃

- 保留当前 `archive/v0.6.0-review-submitted-20260825` 标签不变；
- 新建修复或替代分支，不要移动或覆盖归档标签。

## 3. 下一版本开工检查

1. `git status --short` 必须为空；
2. 确认分支基于已接受的 v0.6.0 Git 提交，而不是本机快照；
3. 冻结下一版本范围后再修改 `miniprogram/package.json` 版本号；
4. 先运行三套基线测试，确认新分支未自带回归：

```bash
(cd miniprogram && npm run check)
(cd admin && npm run check)
(cd backend && npm test)
```

5. 每个外部动作分别留证：上传、设置体验版、真机验收、提交审核、审核通过、正式发布；
6. 不将“上传成功”“体验版可用”“审核通过”和“正式发布”合并为同一状态。

## 4. 需要持续保持的产品与数据边界

- 固定肠道自测路径：`subpkg/health/pages/assessment/index?assessmentType=GUT_REGULARITY`；
- 当前健康问卷记录仅保存在本机，最长 180 天，不跨设备同步；
- 当前肠道 v2 无安全题、无中止分支、无数值维度；
- 所有肠道结果第一条“优先行动”均为纤维补充建议，具体文案按五类结果分别输出；
- 当前五类建议属于结果文案 v5；已提交审核的原候选仍为 v4，重新上传后才会替换平台候选；
- 体验装库存、购买与领取资格由 ROOT 会员商城负责，小程序不自行扣库存或判定订单；
- 活动页面不得在服务端无数据或异常时生成本机模拟活动；
- 首次渠道归因、运营 Banner、页面分享和有赞商品跳转均需保持配置与正式环境一致；
- Launching 只属于启动入口，不应在问卷或应用内普通跳转时闪现。

若下一版本改变上述任一项，需要同步修改实现、自动化测试、隐私/审核口径及归档文档。

## 5. 后端与数据库待办边界

当前 Git 基线包含 069–071 迁移和对应后端能力，但本归档没有证明这些迁移已在生产执行，也没有证明生产服务已部署本归档后端。

下一轮涉及服务端前，应先只读确认：

1. 生产服务实际运行版本和镜像；
2. `schema_migrations` 当前完整状态；
3. 069–071 的迁移计划、回滚和授权是否仍有效；
4. 小程序当前功能是否仍运行在 `LOCAL_DEVICE` 健康数据模式；
5. 运营后台首页 Banner `kicker` 字段与生产接口是否一致。

没有新的明确授权时，不执行生产部署、流量切换或数据库迁移。

## 6. 当前未关闭的外部事项

- 微信审核结果、审核单号和平台提交时间尚未独立回读；
- 正式发布尚未授权或执行；
- v0.6.0 后端生产部署状态需在下一次服务端工作前重新核对；
- 069–071 生产迁移状态需以数据库只读结果为准；
- ROOT 会员商城库存为动态外部状态，仍以商城页面为准。

上述事项不阻止保存本次 Git 归档，但会影响“正式发布完成”和下一版本服务端开发的判断。
