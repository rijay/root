# 20 个批准画板受控截图与逐屏差异验收

验收日期：2026-08-04  
设计基准：Ardot `myRoot 正式上线 UED · R0 · 2026-08-03`，批准页 Node `368:1`  
结论：20/20 个 Section 已完成本地受控截图；5/20 个 Section 完成设计参考图直接对照；总状态仍为 `BLOCK`。

## 受控环境

- 小程序：微信开发者工具 0.3.9，iPhone 12/13 Pro 模拟器，系统视口 390×844，原始截图 602×1300。
- 运营后台：本地 Element Plus Admin + 内存 Store，页面视口 1240×820，CDP 原始 PNG。
- `LOCAL_MOCKED_STATE` 只复现现有页面分支，不代表候选环境数据、真实用户或生产链路。
- 未执行体验版上传、微信审核、内容发布、流量切换、生产数据库写入或云环境变更。

## 逐屏结果

| # | Section | 本地截图 | 参考对照 | 当前结论 |
|---:|---|---|---|---|
| 01 | 欢迎体验 `368:3` | 两张欢迎页 | Ardot 实时回读，导出超时 | 排版与安全区已取证；缺正式摄影背景 |
| 02 | 首页与共用详情 `368:50` | 首页、详情 | Ardot 实时回读，导出超时 | 页面结构已取证；缺正式商品/活动摄影 |
| 03 | 微信登录注册 `369:40` | 登录、注册 | Ardot 实时回读，导出超时 | 页面态已取证；微信隐私授权弹窗待平台态复验 |
| 04 | Root4U 入口与评测 `372:2` | 入口、首题 | Ardot 实时回读，导出超时 | 代表题目使用临时数据；不证明候选问卷版本 |
| 05 | Root4U 结果与安全 `372:14` | 普通结果、安全分流 | Ardot 实时回读，导出超时 | 两种分支已取证；不证明真实评分与处置 |
| 06 | 活动发现与报名 `372:143` | 列表、详情、我的报名访客态 | Ardot 实时回读，导出超时 | 代表活动使用临时数据；正式摄影与报名状态待验证 |
| 07 | 我的访客与会员 `372:240` | 访客、会员、跳转失败 | Ardot 实时回读，导出超时 | 三态已取证；跨小程序跳转待真机验证 |
| 08 | 我的隐私与账号 `372:254` | 关于、隐私账号、注销弹层 | Ardot 实时回读，导出超时 | 视觉与文案已取证；不代表注销已提交 |
| 09 | 后台框架与发布工作台 `372:502` | 1240×820 | Ardot 导出超时 | 零数据态已取证；缺草稿/阻断/回滚代表态 |
| 10 | 内容运营 `372:593` | 1240×820 | Ardot 导出超时 | 空表已取证；缺代表数据和编辑抽屉 |
| 11 | 共用详情编辑 `372:747` | 1240×820 | Ardot 导出超时 | 空表已取证；缺热点与跳转编辑态 |
| 12 | 活动管理 `372:945` | 1240×820 | Ardot 导出超时 | 空表已取证；缺场次和编辑抽屉 |
| 13 | 活动报名记录 `372:1096` | 1240×820 | Ardot 导出超时 | 空表已取证；缺审核、导出和详情态 |
| 14 | 初始化建档 `372:1248` | 1240×820 | Ardot 导出超时 | 12 问列表已取证；缺题目编辑抽屉 |
| 15 | 量表管理 `372:1399` | 1240×820 | Ardot 导出超时 | 空表已取证；缺题目、计分和发布态 |
| 16 | 推荐规则 `372:1550` | 1240×820 | 已直接对照 | 实现为空表；设计为有数据加编辑抽屉，未完成 |
| 17 | 生活方式建议 `372:1701` | 1240×820 | 已直接对照 | 实现为空表；设计为有数据加编辑抽屉，未完成 |
| 18 | 用户查询 `372:1853` | 1240×820 | 已直接对照 | 实现为查询入口；缺结果列表和详情抽屉 |
| 19 | 操作审计 `372:2004` | 1240×820 | 已直接对照 | 已修正英文 `No Data`；缺有数据列表和详情抽屉 |
| 20 | 发布确认 `372:2155` | 1240×820 | 已直接对照 | 弹窗结构对齐；无内容版本且预览未完成，发布按钮保持禁用 |

## 本轮修正

- 操作审计空表文案由 Element Plus 默认英文 `No Data` 改为 `暂无审计记录`，并增加静态校验。
- 后台截图从会裁切为 1240×698 的高层截图接口切换为 CDP 原始截图，全部重拍为真实 1240×820 PNG。

## 仍需完成

1. Ardot 截图/导出服务恢复后，补齐其余 15 个 Section 参考图并逐屏关闭差异。
2. 在本地或候选环境准备后台代表数据，补拍编辑抽屉、详情、发布、审核和回滚状态。
3. 接入正式摄影后重拍欢迎、首页、共用详情与活动视觉。
4. 补微信隐私授权弹窗、会员中心跨小程序跳转、iOS/Android 真机与弱网证据。

机器可校验明细见 `screen-index.json` 与 `visual-review.json`。
